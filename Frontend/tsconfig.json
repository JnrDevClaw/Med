{
  "extends": "./jsconfig.json",
  "compilerOptions": {
    "baseUrl": "src",
    "paths": {
      "$lib/*": ["lib/*"],
      "$components/*": ["components/*"],
      "$stores/*": ["stores/*"],
      "$utils/*": ["utils/*"],
      "$types": ["types/index.ts"],
      "$assets/*": ["assets/*"]
    }
  }
}
